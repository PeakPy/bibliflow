# BibliFlow Engine — Project Skeleton

This repository is a professional skeleton for a Django + DRF + Celery based data ingestion pipeline (BibliFlow).
Folders created: src/, apps/, docker/, compose/, scripts/, tests/

Next steps:
1. Populate settings (src\bibliflow\settings\*)
2. Implement models and migrations
3. Build Docker images and docker-compose up